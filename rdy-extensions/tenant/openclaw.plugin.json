{
  "id": "rdy-tenant",
  "name": "RdyCore Tenant Resolver",
  "version": "2.0.0",
  "description": "Multi-channel tenant resolver with fair-use rate limiting. Resolves WhatsApp, WeCom, and WeCom KF identities to tenants, injects workspace context, and enforces monthly message caps.",
  "configSchema": {
    "type": "object",
    "properties": {
      "pgConnectionString": {
        "type": "string",
        "default": "postgresql://postgres@localhost:5432/rdycore"
      }
    }
  }
}
